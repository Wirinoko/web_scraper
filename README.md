# BCV Scraper

A simple Python package to scrape the official USD exchange rate from the Central Bank of Venezuela (BCV) website.

## Installation

You can install this package directly from its Git repository:

```bash
pip install git+https://github.com/Wirinoko/web-scraper.git
